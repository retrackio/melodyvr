import styled from 'styled-components';

export const Container = styled.p`
  position: absolute;
  right: 0;
  top: 0;
  font-size: 1.4rem;
  padding: 5px 10px;
  background: rgba(255, 255, 255, 0.9);
`;
